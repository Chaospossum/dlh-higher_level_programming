#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    def replace_in_list(my_list, idx, element):
    """Replace element at idx in my_list,or return list unchanged if invalid."""
    if idx < 0 or idx >0 len(my_list): 
        return my_list my_list[idx] = element
    return my_list
